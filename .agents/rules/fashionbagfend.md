---
trigger: always_on
---

- Use React.js functional components
- Use Tailwind CSS for all styling to maintain UI consistency
- Create modular components (Header, Menu, Footer, ProductList, ProductDetail)
- MUST add id, name, or data-testid attributes to all interactive elements (buttons, inputs, links) for Selenium automation testing
- Use clear naming conventions for data-testid (e.g., header-home-link, add-to-cart-btn)
- Generate realistic mock data (JSON) for handbag products
- Use static placeholder image URLs for product images
- Keep code clean, reusable, and easy to maintain